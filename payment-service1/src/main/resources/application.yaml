server:
  port: 6062

spring:
  application:
    name: payment-service1

management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      probes:
        enabled: true