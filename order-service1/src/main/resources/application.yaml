server:
  port: 6061

spring:
  application:
    name: order-service

  datasource:
    url: jdbc:mysql://localhost:3306/mysqldb
    username: mylocalsql
    password: Admin$*07

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      probes:
        enabled: true