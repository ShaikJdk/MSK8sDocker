pipeline {

    agent any

tools {
        // This MUST match the exact Name you typed in Jenkins UI
        maven 'apache-maven-3.9.6' 
    }
	
    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build Jar') {
    steps {
        dir('order-service1') {
            bat 'mvn clean package -DskipTests'
        }
    }
}

        stage('Build Docker Image') {
    steps {
        dir('order-service1') {
            bat 'docker build -t order-service1:latest .'
        }
    }
}

        stage('Deploy To Kubernetes') {
    steps {
        dir('order-service1') {
            bat 'kubectl --kubeconfig=C:\\Users\\SUBHANI\\.kube\\config apply -f k8s/'
        }
    }
}
    }
}